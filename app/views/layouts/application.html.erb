<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Beef Junior Test Application" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <div class="row">
        <div class="large-1 columns"></div>
        <div class="large-7 large-offset-4 columns"><%= link_to 'Name list', root_path %> | <%= link_to 'Log out', destroy_user_session_path, method: :delete %></div>
      </div>
      <hr>
    <% end %>
    <div class="row">
      <div class="large-8 large-centered columns">
 	<%= render 'layouts/messages' %>
	<%= yield %>
      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
